export interface SectionMeta {
  title: string;
  subtitle: string;
  description: string;
  slug: string;
}
export interface SectionData {
  name: string;
  description: string;
  icon: {
    url: string;
  };
}
